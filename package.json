{
  "name": "api.lib.",
  "version": "0.0.1",
  "description": "Training for a library api",
  "license": "ISC",
  "author": "Emil Larsen",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch --env-file=.env server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-validator": "^7.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "globals": "^17.4.0"
  }
}
